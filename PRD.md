# PillPal 当前产品需求文档

- 文档日期：2026-08-23
- 当前阶段：真实家庭授权与站内通知 MVP 已在功能分支实现，等待远程 migration 和双账号验收

## 1. 产品定义

PillPal 帮助慢病家庭把分散的用药、库存、健康指标和复诊记录整理成可追溯的日常记录与 AI 家庭照护周报。当前核心价值不是给出医疗结论，而是让记录者和异地家人在较短时间内看懂“发生了什么、哪些信息缺失、下一步需要确认什么”。

## 2. 两个角色

| 角色 | 核心任务 | 当前产品实现 |
|---|---|---|
| 被照护人 | 维护本人用药计划、打卡、库存、健康与复诊记录；授权和撤销照护人 | 数据归属于本人账户 |
| 照护人 | 查看获授权对象的摘要、生成 AI 家庭照护周报、发送固定服药提醒 | 只读，不可打卡或修改原始数据 |

授权方提交邮箱前需要二次核对；被授权方不需要确认页，而是在使用该邮箱登录后自动生效。一个被照护人可以授权多位照护人，同一账号也可以作为其他人的照护人。

## 3. 状态矩阵

| 状态 | 范围 | 验收证据/边界 |
|---|---|---|
| 已实现 | Magic Link、受保护路由、用药计划、打卡、库存、健康记录、复诊日程；健康页支持最近健康记录 JSON 导出；设置页通用导入/导出未启用 | 代码、RLS 与测试可验证 |
| 已实现 | AI 周报 V2 异步任务、刷新恢复、Schema/Evidence/安全校验 | 工程链路已实现；V2 正式评测质量 Gate 未通过 |
| 本地已实现 | 授权创建、登录自动领取、撤销、多照护人、服务端跨账号摘要和 AI 周报 | 远程 migration 未应用，尚不能称线上可用 |
| 本地已实现 | 通知中心、未读铃铛、Realtime、固定服药提醒、系统通知 | 仅站内通知；无 Push、短信、微信、自由文本和 Cron |
| 本地已实现 | 照护 actions 与 AI `/weekly-report?from=care&subject=...` | 保持待确认≠漏服、健康数据同意、Evidence 和非医疗边界 |
| 不做 | 诊断、处方、用药调整、药物相互作用判断、外部医疗服务闭环 | 超出作品集 MVP 的能力与安全边界 |
| 不做 | 把合成评测包装成医疗效果或真实用户效果 | 评测只证明输出质量控制与工程闭环 |

## 4. 本轮实现：真实家庭授权与站内通知

### 用户故事

作为照护人，我希望在被照护人授权后查看其最近 7 天的照护摘要、生成周报并发送固定提醒；作为被照护人，我希望收到可追溯的站内通知并随时撤销照护权限。

### 已实现范围

- `/care` 展示已授权的被照护人；无授权对象时显示真实空状态。`/care/permissions` 管理授权，个人中心可发现通知与授权入口。
- 原始业务表仍维持本人 RLS；跨账号摘要、AI 上下文和提醒写入均经 Netlify 服务端验证有效授权。
- 四类概览：
  1. 计划剂次，以及已记录服用、明确跳过、未记录三种状态。
  2. 低库存药品数量。
  3. 最近 7 天血压、血糖、体重记录总数。
  4. 近期复诊倒计时。
- actions 按固定优先顺序从确定性事实生成：低库存、3 天内复诊、明确跳过、未记录、无健康记录；没有命中时显示“暂无需要优先确认事项”。
- AI 开启时链接到 `/weekly-report?from=care&subject=<uuid>`；跨账号生成与任务读取均重新校验有效授权，撤销后立即失效。
- AI 关闭时只禁用生成按钮，照护概览仍可使用。

### 已验证边界

- 已跳过与未记录分开呈现，不计算证据不足的完整依从率，也不把未记录写成漏服。
- 健康概览只展示记录数量，不判断正常、异常或病情。
- 不做被授权方确认页、Push、短信、微信、自由文本、远程编辑、代打卡或消息回复。
- migration 尚未应用到远程 Supabase；本地实现不等于线上能力。

## 5. AI 与隐私要求

- 用户首次生成 AI 周报前必须明确同意。
- 同意后，最近 7 天健康样本可能发送给 Qwen：记录时间 `recordedAt`；血压的收缩压、舒张压和可选心率；血糖数值及测量时段 `timing`（缺失或非法值归为 `random`）；体重数值和可选 BMI。也会发送代码计算的执行汇总、匿名药物引用、库存估算、数据缺口和复诊倒计时。
- 不发送身份信息、邮箱、药品名称、剂量、医院、医生、备注或数据库 ID。
- 模型只组织语言；计划剂次、记录状态、库存和趋势等事实由代码计算。
- 不给出诊断、停药、换药、加减量或疾病归因。
- V2 当前质量 Gate 未通过，后续改动必须保留两个 Bad Case 并跑回归，不能通过放松安全规则换取分数。

## 6. 数据与权限

业务表仍通过 RLS 限制为 `auth.uid() = user_id`。新增 `care_authorizations` 与 `notifications` 只给浏览器最小查询权限，所有跨账号数据读取、授权写入、限频、同步和已读写入均由服务端处理。AI job 记录请求人与数据归属人，创建和读取时重新校验授权。

## 7. 本阶段成功定义

本轮成功定义是两账号可完成“授权 → 登录生效 → 只读照护 → AI 周报 → 固定提醒 → 站内通知 → 撤销立即失效”。当前只完成本地实现，仍需迁移、双账号浏览器验收和 Preview，且 AI 模型质量 Gate 仍未通过。
