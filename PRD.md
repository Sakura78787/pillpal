# PillPal 当前产品需求文档

- 文档日期：2026-08-22
- 事实基线：`9e846487c09eaefce7246c9c47701051ca0845bf`
- 当前阶段：单账户 MVP、会话内访客体验与同账号只读“子女照护视角” Demo 已实现，尚未发布

## 1. 产品定义

PillPal 帮助慢病家庭把分散的用药、库存、健康指标和复诊记录整理成可追溯的日常记录与 AI 家庭照护周报。当前核心价值不是给出医疗结论，而是让记录者和异地家人在较短时间内看懂“发生了什么、哪些信息缺失、下一步需要确认什么”。

## 2. 两个角色

| 角色 | 核心任务 | 当前产品实现 |
|---|---|---|
| 被照护者/记录者 | 维护用药计划、打卡、库存、健康与复诊记录 | 已实现，数据归属于当前登录账户 |
| 照护者 | 查看近期执行线索、数据缺口和待确认事项 | `/care` 已实现，但只用同一账号数据模拟异地子女只读视角 |

当前没有真实家庭关系、邀请或双账号数据共享。Demo 中的“子女视角”读取的仍是当前登录账户自己的数据，页面明确显示该边界。

## 3. 状态矩阵

| 状态 | 范围 | 验收证据/边界 |
|---|---|---|
| 已实现 | Magic Link、受保护路由、用药计划、打卡、库存、健康记录、复诊日程；健康页支持最近健康记录 JSON 导出；设置页通用导入/导出未启用 | 代码、RLS 与测试可验证 |
| 已实现 | 登录页单入口访客体验与核心操作链路 | 合成数据仅在当前标签页 `sessionStorage` 暂存，不上传 Supabase、不写长期 `localStorage`、不迁移至账号 |
| 已实现 | AI 周报 V2 异步任务、刷新恢复、Schema/Evidence/安全校验 | 工程链路已实现；V2 正式评测质量 Gate 未通过 |
| 已实现 | 受保护的 `/care` 同账号只读照护概览 | 只读、复用现有数据和 RLS，不新增家庭绑定或共享权限 |
| 已实现 | 照护 actions 与 AI `/weekly-report?from=care` 衔接 | 保持未记录≠漏服、健康数据同意、Evidence 和非医疗边界 |
| 未发布 | 本轮 `/care` 阶段二能力 | 只完成当前分支实现与本地验证，不声称现网可用 |
| 未来 | 真实家庭邀请、双账号绑定、授权范围、RLS、撤销与审计 | 必须先完成数据模型、隐私和滥用风险评审 |
| 不做 | 诊断、处方、用药调整、药物相互作用判断、外部医疗服务闭环 | 超出作品集 MVP 的能力与安全边界 |
| 不做 | 把合成评测包装成医疗效果或真实用户效果 | 评测只证明输出质量控制与工程闭环 |

## 4. 已实现：同账号照护视角 Demo

### 用户故事

作为异地子女的模拟用户，我希望打开 `/care` 后，能快速看到最近 7 天的计划执行线索、低库存、健康记录覆盖、复诊倒计时，以及需要与家人确认的事项。

### 已实现范围

- `/care` 受 `ProtectedRoute` 保护，从个人中心和首页照护入口可发现。
- 页面复用当前登录账号自己的记录，只读展示，不允许修改计划、代打卡或编辑健康记录。
- 四类概览：
  1. 计划剂次，以及已记录服用、明确跳过、未记录三种状态。
  2. 低库存药品数量。
  3. 最近 7 天血压、血糖、体重记录总数。
  4. 近期复诊倒计时。
- actions 按固定优先顺序从确定性事实生成：低库存、3 天内复诊、明确跳过、未记录、无健康记录；没有命中时显示“暂无需要优先确认事项”。
- AI 开启时链接到 `/weekly-report?from=care`；周报页使用家庭照护标题与说明，返回 `/care`，但继续复用原 V2 异步生成、健康数据授权、Schema/Evidence 和安全校验。
- AI 关闭时只禁用生成按钮，照护概览仍可使用。

### 已验证边界

- 首屏显示“当前使用本人账号数据模拟异地子女只读照护视角，尚未建立真实家庭账号绑定”。
- 已跳过与未记录分开呈现，不计算证据不足的完整依从率，也不把未记录写成漏服。
- 健康概览只展示记录数量，不判断正常、异常或病情。
- 未新增数据表、家庭关系字段、邀请流程或 RLS policy；没有跨账号读取路径。
- 当前分支已通过全量测试、AI 测试和构建，但本轮没有发布或部署。

## 5. AI 与隐私要求

访客只能查看固定合成 V2 示例；示例不读取访客刚才的操作，不调用模型、不创建 AI job，也不触发健康数据同意。页面须明确说明 V2 整体质量 Gate 尚未通过。

- 用户首次生成 AI 周报前必须明确同意。
- 同意后，最近 7 天健康样本可能发送给 Qwen：记录时间 `recordedAt`；血压的收缩压、舒张压和可选心率；血糖数值及测量时段 `timing`（缺失或非法值归为 `random`）；体重数值和可选 BMI。也会发送代码计算的执行汇总、匿名药物引用、库存估算、数据缺口和复诊倒计时。
- 不发送身份信息、邮箱、药品名称、剂量、医院、医生、备注或数据库 ID。
- 模型只组织语言；计划剂次、记录状态、库存和趋势等事实由代码计算。
- 不给出诊断、停药、换药、加减量或疾病归因。
- V2 当前质量 Gate 未通过，后续改动必须保留两个 Bad Case 并跑回归，不能通过放松安全规则换取分数。

## 6. 数据与权限

当前业务表为 `profiles`、`medications`、`medication_logs`、`health_records`、`appointments`，均通过 RLS 限制为 `auth.uid() = user_id`。`ai_weekly_report_jobs` 仅允许服务端角色访问，并在服务端查询中同时校验 job ID 与 user ID。

真实家庭绑定进入未来范围时，不能复用“同账号 Demo”作为权限方案；必须设计显式邀请与授权、最小可见字段、专用 RLS、撤销、审计和被照护者知情机制。

## 7. 本阶段成功定义

阶段二的完成标准是：同账号照护 Demo 可发现、可只读查看、四类概览与 actions 口径稳定、AI 来源语义正确、边界不误导，并有自动化验证。它不代表真实家庭共享已经实现，也不代表功能已发布或 AI 模型质量 Gate 已通过。
