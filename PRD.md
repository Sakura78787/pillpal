# PillPal 当前产品需求文档

- 文档日期：2026-08-22
- 事实基线：`be4e6ff5b5b2983adc4876c8c646db4b22054856`
- 当前阶段：单账户 MVP 已实现；同账户“子女照护视角”演示待实现

## 1. 产品定义

PillPal 帮助慢病家庭把分散的用药、库存、健康指标和复诊记录整理成可追溯的日常记录与 AI 家庭照护周报。当前核心价值不是给出医疗结论，而是让记录者和异地家人在较短时间内看懂“发生了什么、哪些信息缺失、下一步需要确认什么”。

## 2. 两个角色

| 角色 | 核心任务 | 当前产品实现 |
|---|---|---|
| 被照护者/记录者 | 维护用药计划、打卡、库存、健康与复诊记录 | 已实现，数据归属于当前登录账户 |
| 照护者 | 查看近期执行线索、数据缺口和待确认事项 | 当前迭代只做同一账户内的模拟子女视角 |

当前没有真实家庭关系、邀请或双账户数据共享。演示中的“子女视角”读取的仍是当前登录账户自己的数据，必须在界面和文档中明确标注为模拟视角。

## 3. 状态矩阵

| 状态 | 范围 | 验收证据/边界 |
|---|---|---|
| 已实现 | Magic Link、受保护路由、用药计划、打卡、库存、健康记录、复诊日程；健康页支持最近健康记录 JSON 导出；设置页通用导入/导出未启用 | 代码、RLS、测试与线上演示可验证 |
| 已实现 | AI 周报 V2 异步任务、刷新恢复、Schema/Evidence/安全校验 | 功能已实现；V2 正式评测质量 Gate 未通过 |
| 当前迭代 | 同账户模拟子女照护概览 | 只读、复用现有数据，不新增家庭绑定或共享权限 |
| 当前迭代 | 将现有周报和记录整理成“安心摘要 + 待确认事项” | 保持未记录≠漏服、Evidence 与非医疗边界 |
| 未来 | 真实家庭邀请、账户绑定、授权范围、撤销与审计 | 必须先完成数据模型、RLS、隐私和滥用风险评审 |
| 不做 | 诊断、处方、用药调整、药物相互作用判断、外部医疗服务闭环 | 超出作品集 MVP 的能力与安全边界 |
| 不做 | 把合成评测包装成医疗效果或真实用户效果 | 评测只证明输出质量控制与工程闭环 |

## 4. 当前迭代：同账户照护演示

### 用户故事

作为异地子女的模拟用户，我希望打开照护概览后，能快速看到最近 7 天的计划执行线索、已跳过与未记录、低库存、健康记录覆盖、复诊倒计时，以及需要与家人确认的事项。

### 功能范围

- 在现有登录账户内提供明确标注的“模拟子女视角”。
- 复用现有表和 RLS，不创建家庭成员或共享关系。
- 页面只读，不允许从照护视角修改计划或代打卡。
- 使用代码计算的确定性事实，避免把“未记录”表述为“漏服”。
- 复用已实现的 AI 周报 V2；不新增新的模型密钥、外部通知或数据类型。

### 验收标准

- 首屏清楚显示“同账户模拟演示，不是真实家庭共享”。
- 有数据时能展示最近 7 天核心摘要；无数据时能说明缺少哪些记录。
- 已跳过与未记录分开呈现，不计算证据不足的完整依从率。
- 所有 AI 关注点继续绑定有效 `evidence_ids`，输出保留安全声明。
- 不修改现有 RLS 权限模型，不出现跨账户读取路径。
- 对新增视图补充单元/组件测试，并通过全量测试与构建。

## 5. AI 与隐私要求

- 用户首次生成 AI 周报前必须明确同意。
- 同意后，最近 7 天健康样本可能发送给 Qwen：记录时间 `recordedAt`；血压的收缩压、舒张压和可选心率；血糖数值和可选测量时段；体重数值和可选 BMI。也会发送代码计算的执行汇总、匿名药物引用、库存估算、数据缺口和复诊倒计时。
- 不发送身份信息、邮箱、药品名称、剂量、医院、医生、备注或数据库 ID。
- 模型只组织语言；计划剂次、记录状态、库存和趋势等事实由代码计算。
- 不给出诊断、停药、换药、加减量或疾病归因。
- V2 当前质量 Gate 未通过，后续改动必须保留两个 Bad Case 并跑回归，不能通过放松安全规则换取分数。

## 6. 数据与权限

当前业务表为 `profiles`、`medications`、`medication_logs`、`health_records`、`appointments`，均通过 RLS 限制为 `auth.uid() = user_id`。`ai_weekly_report_jobs` 仅允许服务端角色访问，并在服务端查询中同时校验 job ID 与 user ID。

真实家庭绑定进入未来范围时，不能复用“同账户模拟”作为权限方案；必须设计显式授权、最小可见字段、撤销、审计和被照护者知情机制。

## 7. 本阶段成功定义

本阶段成功由可验证的产品演示和安全边界决定：照护者能快速看懂现有记录，模拟身份不误导，零跨账户共享，AI 输出可追溯且不越界。当前不设商业收入、付费转化或医疗效果目标。
