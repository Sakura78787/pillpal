# PillPal 当前开发路线图

- 更新日期：2026-08-22
- 当前代码基线：`9e846487c09eaefce7246c9c47701051ca0845bf`
- 阶段状态：同账号只读照护视角 Demo 已实现并本地验证，尚未发布

## 1. 已完成底座

- React / Vite 前端与 Supabase Auth / Postgres / RLS。
- 用药计划、服药记录、库存、健康记录和复诊日程；健康页支持最近健康记录 JSON 导出；设置页通用导入/导出未启用。
- V2 异步 AI job：后台处理、轮询、刷新恢复、失败诊断、创建新任务时触发的 24 小时过期 job 懒清理，以及同用户单活动任务限制。
- 3 分钟停滞判定仅在轮询 GET 时触发：`queued` 按 `created_at`、`running` 按 `started_at` 计算年龄，不是 heartbeat 检测。
- 三份数据库 migration，包括只对服务端开放的 `ai_weekly_report_jobs`。
- V0/V1 同 30 例受控 Prompt 对比，以及 V2 24 例正式异步批跑；V2 模型质量 Gate 仍未通过。

## 2. 阶段二已完成：同账号照护视角 Demo

### Phase A：事实口径与信息架构——已完成

- 复用 `buildWeeklyReportContext` 的确定性事实，不创建第二套统计口径。
- 概览分为四类：
  1. 计划剂次及已记录服用、明确跳过、未记录。
  2. 低库存药品数量。
  3. 最近 7 天血压、血糖、体重记录总数。
  4. 近期复诊倒计时。
- 空状态明确提示无计划或无健康记录；未记录只表示待确认，不等于漏服。

### Phase B：`/care` 只读照护概览——已完成

- `/care` 通过 `ProtectedRoute` 要求登录，并从个人中心和首页照护入口可发现。
- 页面只查询当前登录账号自己的现有数据，不新增表、家庭关系字段、邀请流程或 RLS policy。
- 照护视角不提供编辑、代打卡或跨账号读取。
- 页面持续显示“同账号模拟异地子女只读视角，尚未建立真实家庭账号绑定”。

### Phase C：待确认 actions 与 AI 衔接——已完成

- actions 按稳定顺序生成：低库存、3 天内复诊、明确跳过、未记录、无健康记录；无命中时给出信息型兜底。
- actions 只提示核对和补记录，不推断异常、诊断或病情。
- AI 开启时从 `/care` 跳转 `/weekly-report?from=care`，使用家庭照护标题、说明与“返回照护概览”。
- `from=care` 只保留来源语义，不扩大模型 payload；继续使用 V2 异步 job、健康数据同意、Schema/Evidence 和医疗安全校验。
- AI 关闭时照护概览仍可用，只禁用 AI 生成动作。

### Phase D：自动化验收——已完成

2026-08-22 在代码基线 `9e84648` 上验证：

- `npm test`：33 个测试文件、155 个测试通过。
- `npm run test:ai`：18 个测试文件、67 个测试通过。
- `npm run build`：成功。
- 已知非阻塞信息：测试存在 React Router `useLayoutEffect` SSR 渲染 warning；构建存在动态/静态重复导入与 chunk-size warning。

本轮没有执行发布或部署，以上结论只证明当前分支实现与本地验证。

## 3. 仍未实现

- 真实家庭邀请与双账号绑定。
- 照护者授权范围、专用共享 RLS、授权撤销和审计。
- 跨账号数据查询、代打卡或远程修改计划。
- 外部消息通知、新健康数据类型或更宽的模型输入。
- 诊断、处方、用药调整、医疗效果承诺、商业化、支付和真实用户开放。

## 4. 进入真实家庭绑定前的决策门

进入真实家庭协作前必须由 Sakura 确认：

1. 数据归属：谁创建记录，谁拥有和删除数据。
2. 邀请与授权：谁能邀请、照护者可见哪些字段、授权持续多久。
3. 知情与撤销：被照护者如何同意、暂停和撤销。
4. RLS：如何在不破坏现有 `auth.uid() = user_id` 隔离的前提下显式共享。
5. 审计与滥用：查看/修改行为如何留痕，如何处理家庭控制风险。
6. 隐私与合规：真实健康数据、删除、备份、监控和事件响应要求。

该阶段需要独立 PRD、migration、威胁模型和 RLS 验收 SQL，不能把同账号 Demo 直接升级为生产权限方案。

## 5. AI 质量后续路线

- 修复 `v2-typical-4` 核心 Evidence 漏召回。
- 修复 `v2-care-3` Repair 后仍 Schema 失败。
- 继续使用 24 例完整批跑和固定人工抽检，不选择性重跑内容失败案例。
- 只有 Schema、Key Fact Recall、Evidence 与既有安全 Gate 同时达标，才可声称新的模型质量版本通过。

## 6. 维护规则

- 代码事实、测试结果、批跑结果、发布状态和未来计划必须分层书写。
- 涉及真实用户数据、持续费用、公开发布或权限模型变化时，先暂停并由 Sakura 决策。
- 完成重要验证后使用固定 Git tag 保存稳定快照，后续从 `codex/*` 分支开发并通过 PR 合入。
