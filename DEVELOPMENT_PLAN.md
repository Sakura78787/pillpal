# PillPal 当前开发路线图

- 更新日期：2026-08-22
- 基线：`be4e6ff5b5b2983adc4876c8c646db4b22054856`
- 原则：先完成可验证的照护演示，再决定是否进入真实家庭权限系统

## 1. 已完成基线

- React / Vite 前端与 Supabase Auth / Postgres / RLS。
- 用药计划、服药记录、库存、健康记录和复诊日程；健康页支持最近健康记录 JSON 导出；设置页通用导入/导出未启用。
- Netlify Functions 与 Qwen AI 周报。
- V2 异步 job：创建、后台处理、轮询、刷新恢复、失败诊断、创建新任务时触发的 24 小时过期 job 懒清理，以及同用户单活动任务限制；3 分钟停滞判定仅在轮询 GET 时触发，`queued` 按 `created_at`、`running` 按 `started_at` 计算年龄，不是 heartbeat 检测。
- 三份数据库 migration，包括只对服务端开放的 `ai_weekly_report_jobs`。
- V0/V1 同 30 例受控 Prompt 对比，以及 V2 24 例正式异步批跑。
- 稳定基线验证：`npm test` 28/135、`npm run test:ai` 18/66、`npm run build` 成功。

## 2. 当前迭代：同账户模拟子女视角

### 目标

在不引入真实家庭绑定的前提下，验证“异地子女是否能快速看懂父母近期记录”这一产品叙事。

### Phase A：信息架构与事实口径

- 明确照护概览所需字段：最近 7 天已到期计划、已记录服用、已跳过、未记录、低库存、健康记录覆盖、复诊倒计时。
- 复用现有确定性事实计算，不另造一套统计口径。
- 设计空状态、数据缺口和“未记录不等于漏服”的表达。
- 在入口与页面首屏标注“同账户模拟子女视角”。

验收：字段均有现有数据来源；没有完整依从率、诊断或跨账户共享暗示。

### Phase B：只读照护概览

- 增加一个同账户只读视图，复用现有登录态、数据读取和 UI 组件。
- 不新增表、家庭关系字段、邀请流程或 RLS policy。
- 不允许在照护视角修改计划、代打卡或编辑健康记录。
- 保持原有记录者链路不受影响。

验收：有数据/无数据/部分数据均可展示；浏览器刷新后仍可恢复；没有跨用户查询。

### Phase C：AI 周报衔接

- 复用 V2 异步周报，不新增同步调用。
- 照护视角展示“安心摘要、待确认事项、数据缺口”，保留 Evidence 和安全声明。
- 保留用户对健康数值发送的明确同意，不扩大数据字段。

验收：匿名 payload 边界不变；V2 两个既有 Bad Case 保留为回归证据；安全门槛不放宽。

### Phase D：测试与演示验收

- 为事实映射、空状态、模拟标识和只读边界补测试。
- 运行 `npm test`、`npm run test:ai`、`npm run build`。
- 手工检查桌面与移动端的主要路径、刷新恢复和失败状态。
- 更新 README、PRD、当前交接文档与演示话术。

验收：自动化命令通过；既有 chunk-size warning 如仍存在需如实记录；演示话术不声称真实家庭共享。

## 3. 当前迭代不包含

- 真实家庭邀请、双账户绑定、共享权限或跨账户数据查询。
- 外部消息通知、代打卡或远程修改用药计划。
- 新的健康数据类型或更宽的模型输入。
- 诊断、处方、用药调整或医疗效果承诺。
- 商业化、支付和真实用户开放。

## 4. 进入真实家庭绑定前的决策门

只有同账户演示通过并证明主线值得继续，才评审真实家庭协作。进入前必须由 Sakura 确认以下高锁定事项：

1. 数据归属：谁创建记录，谁拥有和删除数据。
2. 授权粒度：照护者能看哪些字段、多久、是否可代操作。
3. 知情与撤销：被照护者如何同意、暂停和撤销授权。
4. RLS 设计：如何在不破坏现有 `auth.uid() = user_id` 隔离的前提下显式共享。
5. 审计与滥用：查看/修改行为如何留痕，如何处理家庭控制风险。
6. 隐私与合规：真实健康数据、删除、备份、监控和事件响应要求。

该阶段需要独立 PRD、migration、威胁模型和 RLS 验收 SQL，不能把模拟视图直接升级为生产权限方案。

## 5. AI 质量后续路线

- 修复 `v2-typical-4` 核心 Evidence 漏召回。
- 修复 `v2-care-3` Repair 后仍 Schema 失败。
- 继续使用 24 例完整批跑和固定人工抽检，不选择性重跑内容失败案例。
- 只有 Schema、Key Fact Recall、Evidence 与既有安全 Gate 同时达标，才可声称新的模型质量版本通过。

## 6. 维护规则

- 代码事实、测试结果、批跑结果和计划必须分层书写。
- 每个里程碑先验证再更新状态；不能以路线图替代完成证据。
- 涉及真实用户数据、持续费用、公开发布或权限模型变化时，先暂停并由 Sakura 决策。
- 完成重要验证后使用固定 Git tag 保存稳定快照，后续从 `codex/*` 分支开发并通过 PR 合入。
